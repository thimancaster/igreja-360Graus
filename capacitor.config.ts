import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.igreja360hub',
  appName: 'igreja-360-hub',
  webDir: 'dist'
};

export default config;
